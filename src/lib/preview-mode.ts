import { DEMO_MEMORY_CONTAINER } from "@/lib/memory-demo-data";

/** Supermemory container for the shared example persona — not the user's ID. */
export const PREVIEW_MEMORY_CONTAINER = DEMO_MEMORY_CONTAINER;
